# This file is the *measured* environment: the exact versions every number in
# docs/FINDINGS.md was produced against, on Linux with CUDA. It is a lockfile
# for reproducing those results, not the way to install the package.
#
# To install and use it, do this instead -- it works on Windows, macOS and
# Linux, and resolves versions for whatever Python you have:
#
#   pip install -e .              core: load circuits, inspect wiring
#   pip install -e '.[all]'       everything
#
# Ranges live in pyproject.toml. Pin from here only when you are trying to
# reproduce a published figure.
#
# Python 3.12. Newer versions have no wheels for Brian2 / torch / MuJoCo.
#
#   uv venv --python 3.12 .venv
#   uv pip install -r requirements.txt

# Connectome data
neuprint-python==0.6.3
pyarrow==25.0.1
pandas==3.0.5
numpy==2.5.3

# Biophysics and body
brian2==2.10.1
mujoco==3.9.0
flygym==2.1.0

# Learning and optimisation
# Plain PyPI, which ships the CUDA-enabled build on linux x86_64. Do not pin
# the '+cpu' variant here: it contains no CUDA at all, so trainable.py falls
# back to the CPU silently and you lose ~10x. Measured on an RTX 3050 Mobile,
# 4 GB: 8.9x on the mushroom body, 12.3x on LC+LPLC -> DN.
torch==2.14.0
scipy==1.18.1
cma==4.5.0

# Plotting and rendering
matplotlib==3.11.2
pillow==12.3.0
